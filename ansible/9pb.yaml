- name: Looping th installations
  hosts: localhost
  connection: local
  become: yes
  tasks:
    - name: installing the packages
      ansible.builtin.yum:
        name: "{{ item }}"
        state: present
      loop:
        - mysql
        - nginx
    